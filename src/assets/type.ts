export interface BookObj {
    bookId?: number,
    bookName?: string,
    description?: string,
    author?: string,
    price?: number,
    bookImage?: string,
    quantity?: number,
    addedOn?: Date,
    updatedOn?: Date
}
